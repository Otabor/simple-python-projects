def string_len(in_string):
    """
      Implement this function to return the length of the input string 
      Example:
      string_len("Physics") should return 7
      string_len("lol") should return 3
    """
    var = None
    return var


def char_freq_in_string(in_string):
    """
      Implement this function to return the frequency of each character in the input string
      Example:
      char_freq_in_string("minimum") should return m:3, i:2, n:1, u:1
    """
    var = None
    return var


def string_replacement_one(in_string): 
    """
      Implement this function to replace every occurence of the first character of the input string with '£'
      Example:
      string_replacement_one("polo") should return "£olo"
      string_replacement_one("maximum") should return "£axi£u£"
    """
    var = None
    return var


def string_replacement_two(in_string):
    """
      Implement this function to replace every occurence of the first character of the input string with '#',
      except the last occurence of the first character
      Example:
      string_replacement_two("minimum") should return "#ini#um"
    """

